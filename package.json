{
  "name": "limquiz",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-data.js && node scripts/build-changelog.js && node scripts/build-decks.js && node scripts/build-egos.js && node scripts/build-content-updates.js && node scripts/build-ego-gifts.js"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}
